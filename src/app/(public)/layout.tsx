// This file is intentionally left blank.
// The root layout at /src/app/layout.tsx now handles the public layout.
export default function PublicLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
