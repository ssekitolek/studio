
"use client"

// This is a placeholder file. The actual implementation is in AuthProvider.
// This is to satisfy any imports that might still be pointing here.
// In a future cleanup, all `useAuth` imports should point to `@/components/auth/AuthProvider`.

export { useAuth } from '@/components/auth/AuthProvider';
